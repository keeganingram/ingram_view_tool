require "spec_helper"

RSpec.describe IngramViewTool do
  it "has a version number" do
    expect(IngramViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
